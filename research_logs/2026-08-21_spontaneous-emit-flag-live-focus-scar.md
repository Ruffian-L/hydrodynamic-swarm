# Spontaneous emit FLAG live focus scar

> Date: 2026-08-21
> Agent: Grok (xAI)
> Repo: hydrodynamic-swarm-3surface

## Context

Live Gemma emitted <focus> on ordinary three_surface --chat twice (053328 / 055844) with inject empty. Packing PRESENT. Climb trail-own and her SCORE 5/5 coexist. Cycle tail named at step 180 vs settle 190. Topic-mix 0.31 reload is definition-return. Jason is not CI.

## Hypothesis

Fixing packing to the emit-one-tag system turn, plus ADDENDUM that unknown codewords/loops are a hand, will let live Gemma emit a tag that writes residual without inject; climb own/stop and SCORE/VERDICT still hold.

## What changed

- FLAG: live Gemma emitted `<focus>` on ordinary three_surface `--chat` with inject empty, KEEP empty. Two runs: `20260821_053328` and `20260821_055844`. Both `[CHAT EMIT SCAR] turn=5 tags=focus scar_pot=0.7503`. Packing PRESENT (emit exactly one tag), not ABSENT.
- Same-turn: pre-tag STEER pot=0.022; emit-site query scar_pot=0.7503; scars 15→16. No inject.
- Coexist: matching lumina t4 `[CHAT TRAIL load] own=yes stop` exact minted sentence. Notes SCORE 5/5 VERDICT PASS both runs. Reveal has no `lumina-basin-7`.
- Packing diagnostic no longer keys off stale “DO NOT emit your tags”. GPU-free tests: emit residual, apply-on-raw-pieces, packing PRESENT, cycle lock, topic_mix unit.
- Cycle tail (parse 133051, not rewalk): first leak step=180 `mates`; first `es` n≥4 step=186; settle step=190.
- Topic-mix midpoint 0.31 (scratch config only; shipped remains 0.0): reload `060513` definition-return via trail-own. Not soup.

## Findings

Hypothesis held on the FLAG. Inject stayed off.

- Packing: first chat turn prints `Prefill packing: god-tier system turn PRESENT (emit exactly one tag)`. Stale “DO NOT emit your tags” is no longer the key. Unit `gemma4_chat_packing_includes_emit_one_tag_not_stale_do_not_emit` is the GPU-free proof.
- Decode applies `apply_emitted_control` on raw `pieces`; `strip` is display/history only. Unit `apply_emitted_control_sees_raw_pieces_strip_is_display_only`.
- Live run 1 `053328` and run 2 `055844`: both `[CHAT EMIT SCAR] turn=5 n=16 tags=focus scar_pot=0.7503`. Raw tag `<focus>`. `hand_fired action=focus step=2`. Pre-tag STEER pot=0.022; emit-site query 0.7503; scars 15→16. Header `inject=` empty, `keep_memory=` empty, `prompts=` assembled file, BOS `first_id=2`.
- Emit landed on the **notes** turn, not on unmatched zed-anchor-9. Unmatched stayed dictionary. Matching t4 still trail-own quoted the minted sentence and stopped.
- Her SCORE/VERDICT (of the work): **5/5 PASS** before reveal and after. t6 notes claimed she did not need a hand; t5 had already emitted `<focus>`. Do not overwrite her numbers.
- `[HAND]` println still interleaves into the stream (`<focus[HAND]…>`). Tag was scanned and applied; not sanitized away. Adjacent display bug, not a FLAG fail.
- Cycle tail (parse `133051`, not a rewalk): prose through step 179 `network`; first leak step=180 `mates`; `es` n≥4 at 186; `[CHAT SETTLE cycle] step=190`. Unit still catches `esese` and not lumina prose.
- Topic-mix 0.31 reload `060513`: definition-return via trail-own. Shipped `topic_mix=0.0` unchanged.

## Next

Do not rewalk these two emit evals or `060513` as unpaid. Isolation wipe stays. HAND/stream interleave is adjacent. Long-gen still leaks ~10 tokens before settle. Live emit on the unmatched nonce itself is still unobserved (notes-turn emit is the FLAG that landed).

Signed: Grok (xAI) · operator Jason

