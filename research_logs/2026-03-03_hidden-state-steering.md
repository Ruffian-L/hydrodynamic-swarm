# Research Entry: Vendor Quantized Llama & Add Hidden-State Steering (Phase 2.1)

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: `d7f194e32a4373723a15f1353158c89421afe0f9` | Date: 2026-03-03 | Author: ruffian

---

## Commit Summary

- **Message**: feat: vendor quantized_llama, add hidden-state steering (Phase 2.1)
- **Files changed**: 4
- **Insertions/Deletions**: +598 / -28

## What Changed

This commit introduces a major architectural shift by moving the physical steering mechanism from logit space to the pre-lm_head hidden states (D-dimensional space).

### `src/llama.rs`
- **What was modified**: Vendored `quantized_llama.rs` from `candle-transformers`. Added `forward_hidden()`, `forward_with_hidden()`, and `project_to_logits()` methods. A shared `run_layers()` was introduced to avoid code duplication.
- **Why**: The candle-transformers library only returned logits from the final `lm_head`. Vendoring and extending the model exposes the D-dimensional hidden states, which represent a more dense and physically meaningful space for Niodoo to apply forces.
- **Relation to Architecture**: Directly supports Phase 2.1, providing the foundational hooks needed to steer the residual stream before token projection.

### `src/config.rs`
- **What was modified**: Added a new boolean flag `steer_hidden` to `PhysicsConfig` (defaulting to `true`).
- **Why**: Allows toggling between the new hidden-state steering and the legacy logit-space steering for A/B testing or fallback.

### `src/main.rs`
- **What was modified**: The generation loop now handles both `steer_hidden=true` (using `forward_with_hidden`) and `steer_hidden=false` (using `forward`). The goal attractor is now extracted directly from the hidden state (D-dim) rather than from the first D logits. Finally, when `steer_hidden` is true, the steered hidden state is projected back to logits using `project_to_logits()` before temperature sampling.
- **Why**: Wires the new hidden-state capabilities through the core runtime loop.
- **Relation to Architecture**: Modifies the core runtime loop defined in `FOUNDATION.md`. The "particle sliding down a continuous Diderot field" now operates in the model's native hidden dimension space, rather than an arbitrary slice of the logit vocabulary.

## Physics Impact

- **Steering (niodoo.rs & main.rs)**: The fundamental space where physics calculations (gradient, splat force, goal force) are computed has changed. The `goal_pos` and the particle's `query_pos` are now D-dimensional dense vectors representing the transformer's final hidden layer output. This is a much more meaningful physical space than raw vocabulary logits, meaning the Diderot field and splat memory "scar tissue" now operate on the model's true semantic representations.

## Key Findings

1. **Hidden-State Steering Metrics**
   - Evidence: In the telemetry log `2026-03-03_01-51-39_unsloth_v3-forcecap80_T0_9_s150_a2_d100.jsonl`, a generation run with hidden-state steering yielded a `delta_mean` of 19.812191, `delta_max` of 37.2993, and `delta_min` of 0.21086518. The `goal_attractor_norm` was recorded as 195.819.
   - Significance: These metrics show that steering in the hidden state is actively applying measurable deltas to the residual stream without causing extreme blowups (respecting the 35.0 force cap stabilized in the previous commit). The forces are effectively perturbing the D-dimensional space.

## Experiment Progression

- **Before**: Steering forces were applied to a slice of the logit vocabulary, which was an imprecise proxy for semantic meaning.
- **After**: The physics engine now steers the pre-lm_head hidden states directly, operating on dense, semantic representations.
- **Next**: Evaluate the qualitative impact of hidden-state steering compared to logit-space steering. Ensure micro-dreams and TopoCoT remain stable under the new dimensional space.