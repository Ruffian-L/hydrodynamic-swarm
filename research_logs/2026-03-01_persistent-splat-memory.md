# Research Entry: Persistent Splat Memory via Safetensors

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: `411b41989c0dca9433cc8ba1517917dc722519b8` | Date: 2026-03-01 | Author: ruffian-l

---

## Commit Summary

- **Message**: feat: persistent splat memory via safetensors
- **Files changed**: 3
- **Insertions/Deletions**: +144 / -14

## What Changed

This commit introduces persistence for splat memory, allowing the accumulated "scar tissue" to survive across process boundaries. It also improves telemetry by logging the model variant at every step.

### Memory (src/memory.rs)

- Added `SplatMemory::save()` to serialize existing splats (extracting `mu`, `sigma`, and `alpha` tensors) to `data/splat_memory.safetensors`.
- Added `SplatMemory::load()` to reconstruct splats from disk on startup, appending them to the active memory pool.
- This fulfills the Phase 3 goal of making the memory layer a true long-term scar tissue component.

### Generation (src/main.rs)

- `main()` now attempts to load persistent splat memory via `engine.memory_mut().load(splat_file)` at startup.
- `main()` now saves the persistent splat memory back to disk before the Phase 6 Dream Replay wipes or alters them significantly via decay.
- It also sets up logging to record the `model_variant`.

### Logger (src/logger.rs)

- Modified `LogEntry` and `SessionLogger` to store and output `model_variant` in every JSONL log entry ("config", "step", "summary"), increasing log traceability.

## Physics Impact

- **Memory (memory.rs/splat.rs)**: The introduction of `save()` and `load()` ensures that the forces (pulls and pushes) from historical splats are applied to the particle right at step 0 of new generations. The splat distribution (density scalar and vector slope) is no longer a blank slate upon process restart.
- **Generation (main.rs)**: The steering pipeline now experiences non-zero baseline `splat_force` right out of the gate, altering the trajectory significantly compared to a cold start.

## Key Findings

1. **Memory is actively pulling from step 0**
   - Evidence: The commit notes state that Run 2's delta jumped from ~0 to 80.38 at step 0.
   - Significance: This proves that the saved "scar tissue" correctly biases the particle's initial trajectory in the field. The loaded splats effectively function as a permanent, accumulated context over many sessions.

## Experiment Progression

- **Before**: Splat memory was ephemeral; every run or application restart started with a blank slate, requiring the model to regenerate splats from scratch to form memory.
- **After**: Splats persist across runs, creating a continuous, cumulative Diderot field landscape over the lifetime of the application.
- **Next**: Analyze the long-term impact of memory accumulation (e.g., does the field become too dense over time? Do we need to introduce a global decay or limit the maximum number of loaded splats?).

## Open Questions

- [ ] Does the unbounded accumulation of splats in `data/splat_memory.safetensors` eventually cause memory exhaustion or performance degradation during `query_force` computation?
