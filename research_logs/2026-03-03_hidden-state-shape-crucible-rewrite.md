# Research Entry: Resolve 3D hidden-state shape bug and rewrite crucible runner

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: `ab40b6c1fdefd6b72843d8e396b872a85f4c9479` | Date: 2026-03-03 | Author: ruffian

---

## Commit Summary

- **Message**: fix: resolve 3D hidden-state shape bug, rewrite crucible runner
- **Files changed**: 9
- **Insertions/Deletions**: +18 / -4

## What Changed

This commit resolves a tensor dimension bug introduced in the Phase 2.1 transition to hidden-state steering and significantly improves the Crucible runner.

### src/llama.rs

- Modified `run_layers` to use `.narrow(1, seq_len - 1, 1)?.squeeze(1)` instead of `.i((.., seq_len - 1, ..))`. This ensures the returned tensor has shape `(b_sz, hidden_dim)` (1, D), strictly eliminating any phantom sequence dimension from leaking into the steering force calculations.
- This relates directly to Phase 2.1 (Architect's Bridge) where steering acts on the D-dimensional hidden state rather than the V-dimensional logit space. Ensuring strict adherence to D dimensions prevents broadcasting errors and inaccurate force compositions in the Niodoo physics engine.

### src/main.rs

- Removed a spurious `.unsqueeze(0)?` in the `steer_input` assignment because the hidden state from `forward_with_hidden` is already correctly sized to `(1, D)`.

### Crucible Runner & Telemetry

- Rewrote the Crucible baseline runner as a Rust binary (`src/bin/crucible.rs` according to previous commits, with updates reflecting live filtering).
- Added live token streaming and robust filtering of telemetry noise. The logs (like `logs/live.txt` and `logs/crucible_*`) now show clean, decoded outputs with clear test separators.

## Physics Impact

- **Steering (niodoo.rs)**: The correction in `llama.rs` and `main.rs` guarantees the `steer_input` provided to the Niodoo engine is exactly shape `(1, D)`. This is essential for accurate computation of `grad_force`, `splat_force`, and `goal_force`.

## Key Findings

1. **Telemetry Stability Under Corrected Dimensionality**
   - Evidence: Telemetry from `logs/2026-03-03_10-52-12_unsloth_v3-forcecap35_T0_9_s35_a2_d100.jsonl` under the 35.0 force cap shows a mean steering delta of ~66.92 and a max steering delta of ~114.90.
   - Significance: The steering forces are actively pushing the hidden state, and the dimensionality fix ensures these deltas are applied to the correct tensor axes without unintended broadcasting side-effects.

## Experiment Progression

- **Before**: Steering on the hidden state occasionally carried a phantom sequence dimension, and the Crucible runner produced noisy telemetry interlaced with token output.
- **After**: Hidden state steering operates strictly in `(b_sz, hidden_dim)` space. Live evaluation streams clean text, dramatically improving the DX of monitoring Crucible runs.
- **Next**: Analyze the qualitative impact of accurate hidden-state steering on the Crucible baseline suite. Evaluate if the 35.0 force cap remains optimal with the dimension bug resolved.
