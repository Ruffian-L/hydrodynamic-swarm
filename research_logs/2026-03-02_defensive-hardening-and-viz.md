# Research Entry: Defensive Hardening and Viz Features

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: `e9da63232d9513d4f4eb0cfc1ad4cca97ac4fc1b` | Date: 2026-03-02 | Author: Ruffian-L

---

## Commit Summary

- **Message**: fix: defensive hardening across 6 modules
- **Files changed**: 6
- **Insertions/Deletions**: +1624 / -181

## What Changed

Describe the architectural and functional changes introduced by this commit.
Group by component when multiple files changed.

### `src/field.rs`

- Guarded `select_nth_unstable_by` against `k=0` or empty indices.
- This prevents a panic when `nearest_tokens` is called with a zero `k` or on an empty field, ensuring stable execution during neighbor lookups.

### `src/config.rs`

- Changed `Config::load` to return `Result<Self, String>` and added rigorous validation for all numeric invariants post-deserialization (e.g., `dt > 0`, `splat_sigma > 0`, `max_tokens > 0`, `temperature >= 0`).
- Prevents invalid TOML configurations from causing NaN or division-by-zero errors downstream in the physics simulation.

### `src/viz.rs` and `src/viz_metal.rs`

- Renamed `TokenNeighbor.similarity` to `probability` to accurately reflect its semantics (softmax probability instead of cosine similarity).
- Added runtime validation in `VizCollector` to ensure `field_positions_flat.len()` matches the expected layout (`vocab * dim`).
- Added all-zero detection in `export_json()` to omit degenerate (collapsed) 3D field point projections with a warning, preventing rendering glitches.
- `viz_metal.rs` was heavily updated, upgrading from WebGL to a Canvas 2D scene, adding token attraction orbs, step-by-step replay, and various layer toggles (field, trail, splats, tokens, ridge).

### `src/gpu.rs`

- Replaced `panic!()` in `map_async` closures with `eprintln!()` warnings, preventing GPU readback errors from crashing the entire process.
- Guarded `batch_field_gradient` against `m == 0` in both CPU and Metal fallback paths to prevent a `Tensor::cat` panic on an empty vector.
- Padded the WGSL `Params` struct from 20 to 32 bytes (adding 3 `u32` padding fields) to satisfy 16-byte uniform alignment requirements and match the host-side upload array.

### `src/logger.rs`

- Updated the `session_id` generation to include an `HH-MM-SS` timestamp component.
- This ensures that each run gets a truly unique ID, preventing collisions when running multiple sessions on the same date with the same label.

## Physics Impact

How does this commit affect the core physics pipeline?
Reference specific forces, parameters, or dynamics:

- **Field (field.rs)**: Stabilizes `nearest_tokens` operations within the continuous field, ensuring robust gradient probes.
- **Steering (niodoo.rs)**: (Indirectly affected) Improved config validation ensures safe values for `dt` and forces are used.
- **Generation (main.rs)**: Improved stability against configuration and GPU errors.

## Key Findings

1. **Robustness is Critical for Swarm Execution**
   - Evidence: Addition of boundary checks, GPU readback warnings, and strict config validation.
   - Significance: As the project scales, handling edge cases (like zero splats or invalid parameters) is crucial to prevent cascading failures in the physics simulation.
2. **Telemetry Validation (Run: `2026-03-02_04-20-01_unsloth_v3-forcecap80_T0_9_s150_a2_d100.jsonl`)**
   - Evidence: The telemetry shows a stable run with `delta_mean` around `17.84` and `delta_max` at `33.48`, with 32 splats retained.
   - Significance: The defensive hardening hasn't negatively impacted the core physics properties of the `unsloth` baseline.

## Experiment Progression

Where does this commit sit in the project timeline?

- **Before**: The codebase was prone to crashes from invalid configs, empty tensor concatenations, and unaligned GPU memory operations. Visualization was basic WebGL.
- **After**: The project is significantly more resilient. Configurations are validated, GPU operations are safer, and the visualization tool (`viz_metal.rs`) is much more feature-rich and robust.
- **Next**: With a stable and observable runtime, further experimentation on `TopoCoT` events and micro-dreams can proceed with higher confidence.
