# Research Entry: Config Wiring and Memory Museum

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: `b5770e0ac5159435a6249a710c75f49c9fd0ef6b` | Date: 2026-03-03 | Author: ruffian <ruffian@mac.local>

---

## Commit Summary

- **Message**: feat: wire up config.rs, add Memory Museum CLI, replace all hardcoded constants
- **Files changed**: 5
- **Insertions/Deletions**: +221 / -36

## What Changed

### `src/main.rs` & `src/tui.rs`
- **Configuration Wiring**: Removed 15+ hardcoded magic numbers (e.g., dt=0.08, viscosity_scale=0.35, temperature=0.9, EOS token IDs 128009/128001). Replaced them with a dynamically loaded `Config::load("config.toml")` which safely falls back to defaults.
- **Memory Museum**: Introduced an interactive CLI prompt at the end of the generation sequence. Users can now choose to save the resulting splat memory (the "exhibit") as a `.safetensors` file alongside its metadata to a new `exhibits/` directory, or "toss" it.

### `src/config.rs`
- **Validation and Defaults**: Added `eos_token_ids` to the `GenerationConfig`. Added 5 new unit tests to ensure `Config::default()` validates, TOML parsing handles missing fields correctly, and validation catches negative values like `dt = -1.0` or `max_tokens = 0`.

### `src/niodoo.rs`
- **Engine Initialization**: Modified `NiodooEngine::new` to explicitly accept `dt`, `viscosity_scale`, and `force_cap` as arguments rather than hardcoding them internally.
- **Telemetry Accessors**: Added `field_kernel_sigma()` and `field_n_points()` to expose field parameters for telemetry logging.

## Physics Impact

- **Steering (niodoo.rs)**: The engine's core physics parameters (`dt`, `viscosity_scale`, `force_cap`) are now dynamically injected. This directly maps to Phase 2 (Continuous Field) by allowing configuration-driven tuning of the Diderot field without recompilation.
- **Memory (memory.rs/splat.rs)**: Consolidation and pruning thresholds are now configurable (`cfg.memory.consolidation_dist` replacing 80.0, and `cfg.memory.max_splats` replacing 500).
- **Generation (main.rs/tui.rs)**: Micro-dreams (Phase 4) triggers are now dictated by `cfg.micro_dream.fixed_interval` and `cfg.micro_dream.entropy_threshold`, rather than hardcoded step checks (e.g. `step % 25 == 0`). Splat creation also utilizes `cfg.physics.splat_delta_threshold` and `cfg.physics.min_splat_dist`.

## Key Findings

1. **Flexible Steering Dynamics**
   - Evidence: `engine = NiodooEngine::new(field, memory, backend, cfg.physics.dt, cfg.physics.viscosity_scale, cfg.physics.force_cap);`
   - Significance: This enables rapid parameter sweeping across different models and use cases without changing the source code, accelerating evaluation suites like Crucible.

2. **Memory Museum Curation**
   - Evidence: `std::fs::copy(splat_file, &exhibit_path)?;`
   - Significance: By allowing users to save specific `.safetensors` and `.meta.json` pairs, the project now supports curating a library of interesting "scar tissue". This separates ephemeral session generation from long-term memory analysis.

## Experiment Progression

- **Before**: The codebase was littered with hardcoded physics and generation parameters, making A/B testing and rapid iteration difficult. Memories were saved blindly without an easy curation process.
- **After**: A fully configuration-driven execution pipeline with strong validation to prevent panic-inducing inputs (like negative dt). The introduction of the Memory Museum provides a mechanism to archive compelling trajectories.
- **Next**: Run new sweeps with varied force caps and micro-dream settings using the new config system. Build out further Memory Museum features to potentially inject saved exhibits as pre-existing memory for new sessions.

## Open Questions

- [ ] How will the configurable EOS tokens impact generation across different model variants (e.g., `bert` vs `unsloth`)?
- [ ] Should the Memory Museum support loading a saved exhibit at the start of a session as pre-existing scar tissue for Phase 3?