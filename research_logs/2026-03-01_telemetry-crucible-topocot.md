# Research Entry: Splat Force Telemetry, Crucible Eval Suite, and TopoCoT

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: 6e2a2fb54469c91a48d5d71bfc62dfe752cf4285 | Date: 2026-03-01 | Author: ruffian-l

---

## Commit Summary

- **Message**: feat: splat force telemetry + Crucible eval suite + TopoCoT dream correction
- **Files changed**: 5
- **Insertions/Deletions**: +141 / -13

## What Changed

This commit introduces three significant capabilities to the Swarm: high-resolution force telemetry, a formalized baseline evaluation suite, and a metacognitive event trigger called TopoCoT.

### CRUCIBLE.md

- Added the "Swarm Crucible: Phase 2 Baseline Suite," comprising 8 standardized prompts explicitly designed to stress-test specific dimensions of the Swarm.
- The 8 categories are: Spatial AGI, The Trap (False Premise), Agentic State-Machine, TopoCoT Metacognition, Technical Architect, Pure Math/Logic, Deep Context Needle, and Creative Fluidity.
- Provides a structured scoring metric based on Coherence (0-10), Accuracy (0-10), Hallucination (0-10), Splat Influence (splat_force_norm > 0 at step 0), and Dream Corrections (count of [DREAM_CORRECTION] events).

### src/dream.rs

- Introduced `DREAM_CORRECTION_THRESHOLD` set to `5.0`.
- Modified `micro_dream` to return a `MicroDreamResult` struct. This struct includes the `consolidated` position, the `correction_norm`, and a `reflection_triggered` boolean.
- When `correction_norm` exceeds `5.0`, it flags a "hydraulic jump"—a significant trajectory warp where the model essentially hits a wall and must course-correct. This reflection is dubbed TopoCoT.
- The micro-dream forward projection was modified to use a deterministic offset (`1000 + step * 10 + fwd`) during the `steer` call to ensure clear force logging during projection steps.

### src/niodoo.rs

- The `steer` function now accepts the current generation `step`.
- Added high-resolution force telemetry: `splat_mag`, `grad_mag`, and `goal_mag` are now calculated by computing the L2 norm of their respective force tensors (`tensor.sqr()?.sum_all()?.sqrt()`).
- These magnitudes are logged for steps <= 15 and every 10th step thereafter, allowing for direct observation of how splat scar tissue warps the latent trajectory in real-time.

### src/main.rs

- Integrated the `MicroDreamResult` into the generation loop.
- When `reflection_triggered` is true, the system outputs a `[TOPO-COT]` marker, explicitly signaling a latent path recalibration to the console.

## Physics Impact

- **Steering (niodoo.rs)**: We can now independently measure the magnitude of the three core forces driving the particle: the Diderot field ridge gradient (`grad_force`), the scar tissue pull/push (`splat_force`), and the prompt attractor (`goal_force`).
- **Memory (dream.rs)**: The introduction of the 5.0 correction threshold operationalizes the concept of a "hydraulic jump." When the latent particle is abruptly redirected by physics forces (resulting in a high correction norm during a micro-dream), the system recognizes this structural discontinuity.

## Key Findings

1. **Hydraulic Jump Detection**
   - Evidence: The commit message notes the first hydraulic jump was detected at step 8 with a `correction_norm` of 9.22.
   - Significance: This proves that the splat forces are capable of inducing massive, sudden shifts in the latent trajectory that exceed the 5.0 threshold. The Swarm is actively fighting to course-correct the LLM.

## Experiment Progression

- **Before**: Micro-dreams quietly blended corrections back into the residual stream without quantifying the magnitude of the course-correction or explicitly identifying major trajectory warps. Force magnitudes were opaque.
- **After**: We have explicit, quantified telemetry for all three physical forces, a threshold for detecting massive trajectory shifts (hydraulic jumps), and a standardized 8-prompt Crucible to baseline these effects across models.
- **Next**: Run the Crucible baseline against multiple model variants (e.g., Unsloth vs. BERT) and analyze the force telemetry logs to correlate hydraulic jumps with specific prompt challenges.

## Open Questions

- [ ] Does the 5.0 `DREAM_CORRECTION_THRESHOLD` trigger too frequently or infrequently across the diverse Crucible prompts?
- [ ] How do the relative magnitudes of `grad_force`, `splat_force`, and `goal_force` evolve during a hydraulic jump?
