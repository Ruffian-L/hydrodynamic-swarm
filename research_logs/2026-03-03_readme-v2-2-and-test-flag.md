# Research Entry: README Rewrite for v2.2 and Test Flag

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: 4c2386b67a973d5ba6bdf983b27991872315c9e6 | Date: 2026-03-03 | Author: Ruffian-L

---

## Commit Summary

- **Message**: docs: rewrite README for v2.2, add --test flag
- **Files changed**: 6
- **Insertions/Deletions**: +94 / -61

## What Changed


### Documentation (README.md)

- The README has been substantially rewritten to reflect the current Phase 2.2 architecture.
- Core new features documented include the live Diderot field extraction from GGUF token embeddings (eliminating the need for the large 2GB `universe_domain.safetensors`), hidden-state steering, renormalization, and the evaporation engine (time-degrading splat memory).
- Default configuration values have been updated to reflect recent stabilization commits: `dt=0.04`, `force_cap=8.0`, `viscosity=0.15`, `gradient_topk=512`, `steer_hidden=true`.
- Documentation for the Crucible (8-Prompt Test Suite) and its usage has been added.

### Execution & CLI (src/main.rs, src/bin/crucible.rs)

- A new `--test` CLI flag was introduced to bypass interactive inputs (specifically, the Memory Museum save/toss prompt at the end of generations).
- The Crucible runner (`src/bin/crucible.rs`) now automatically passes the `--test` flag to individual runs, allowing the suite to run headlessly without manual intervention.

## Physics Impact


- **None directly in code**. However, the commit officially normalizes the updated safe physics boundaries (`dt=0.04`, `force_cap=8.0`) in user documentation, which were established in prior commits to control runaway trajectories and prevent cumulative drift off the Llama manifold during hidden-state steering.

## Key Findings

1. **Successful Test Flag Integration**
   - Evidence: The logs for Crucible run (e.g., `logs/2026-03-03_11-57-44_unsloth_v3-forcecap8_T0_9_s35_a2_d100.jsonl`) generated post-commit show successful completion without hanging on the Museum prompt.
   - Evidence: In the aforementioned JSONL log from a spatial reasoning Crucible test, the model maintained an average steering delta of `~108.2` and a maximum of `~327.2` per step under the new `force_cap=8.0` regime.
   - Significance: The test suite is now fully automated, enabling easier evaluation of future physics modifications.

## Experiment Progression


- **Before**: The README was severely out of date, describing the Phase 1.1 logit-space steering and relying on a broken offline safetensors embedding file. The Crucible required manual intervention or hacks to bypass the Museum prompt.
- **After**: Documentation is aligned with the Phase 2.2 reality. The test pipeline is fully automated and headless.
- **Next**: With the test suite and documentation stabilized, focus can shift to Phase 2.3 (Architect's Bridge embedding steering) or Phase 2.5 (Live WebSocket Visualization).
