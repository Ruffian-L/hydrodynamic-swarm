# Research Entry: Correct Force Cap Mismatch and TUI Generation Loop

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: `bd6d213efc104eca649e427caf6cfcd996bb1429` | Date: 2026-03-03 | Author: ruffian

---

## Commit Summary

- **Message**: fix: correct force cap mismatch (80->35) and TUI generation loop off-by-one
- **Files changed**: 2
- **Insertions/Deletions**: +13 / -16

## What Changed

This commit synchronizes the terminal UI (`tui.rs`) and core generation (`main.rs`) steering configurations, fixing parameter drift and an off-by-one alignment issue in the token loop.

### `src/main.rs`
- **What was modified**: The default `force_cap` parameter passed to `SessionConfig` (and embedded in `test_label`) was lowered from `80.0` to `35.0`.
- **Why**: This corrects a mismatch where the intended force cap was out of sync with other components. A cap of `80.0` was allowing for overly aggressive physical perturbation of the residual stream, whereas `35.0` stabilizes the trajectory while still permitting splat-driven steering.

### `src/tui.rs`
- **What was modified**: The token generation loop was restructured.
- **Why**: The previous implementation suffered from an off-by-one error where the model's `forward` pass was called at the *start* of the loop, redundant with the initial `prefill_logits`. The new structure:
  1. Captures `prefill_logits` outside the loop to seed step 0.
  2. Runs the steering physics (`llama.steer`) on the current logits.
  3. Samples the next token.
  4. Calls `llama.forward(&next_input, ...)` at the *end* of the step to prepare `raw_logits` for the *next* iteration.
- **Relation to Architecture**: This aligns the `tui.rs` logic exactly with the established pattern in `main.rs`, ensuring the physics engine (`Niodoo`) receives the exact same sequence of residual states regardless of which interface is used.

## Physics Impact

- **Steering (`niodoo.rs`)**: By reducing `force_cap` from `80.0` to `35.0`, the maximum magnitude of the total force (`grad_force + splat_force + goal_force + momentum + noise`) applied to the embedding particle in a single time step (`dt`) is more than halved. This drastically tightens the trajectory bounds along the Diderot field, preventing explosive departures from the baseline semantic space. It ensures the steering force is a gentle nudge rather than a violent displacement.

## Key Findings

1. **Stabilizing the Residual Stream**
   - Evidence: The configuration change to `force_cap: 35.0` (matching `splat_sigma: 35.0`).
   - Significance: Earlier runs with `force_cap` set to `80.0` allowed `splat_force` to dominate the generation process, potentially pushing the hidden states into semantically incoherent regions. Capping the total force at `35.0` maintains generation stability while preserving the desired scar-tissue guidance.

## Experiment Progression

- **Before**: The TUI had an off-by-one execution divergence from the main script, and generation configurations were utilizing an overly permissive force cap (80.0), leading to less stable steering.
- **After**: Both main CLI and TUI now use an identical prefill-steer-forward execution loop, and the default force cap is stabilized at 35.0.
- **Next**: Evaluate output coherence and TopoCoT triggering frequencies across the Crucible test suite with the new 35.0 force cap.
