# Research Entry: cargo suite hooks + comment out TUI + qwen35 debug

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: `63743ad848bb66ef054314f286efa079ef02a966` | Date: 2026-03-04 | Author: Ruffian-L

---

## Commit Summary

- **Message**: chore: cargo suite hooks + comment out TUI + qwen35 debug
- **Files changed**: 4
- **Insertions/Deletions**: +61 / -4

## What Changed

The focus of this commit is on tooling, debugging, and gracefully degrading broken features.

### Tooling/Audit (`audit.toml`)
- Added `audit.toml` to define an explicit vulnerability policy for the project.
- Ignored three known-acceptable transitive dependency advisories (`RUSTSEC-2026-0002` via ratatui, `RUSTSEC-2025-0119` via tokenizers, and `RUSTSEC-2024-0436` via ratatui/metal/tokenizers/gemm). These ignore rules allow the `cargo audit` pre-commit hook to pass while still blocking commits that introduce new vulnerabilities.

### TUI (`src/main.rs`)
- The `tui` module was commented out entirely because it is half-implemented.
- Invoking the `--chat` flag now prints a helpful error message (`[!] --chat / TUI mode is currently disabled. Run without --chat for CLI generation.`) instead of crashing the program.

### Qwen3.5 Debugging (`src/qwen35.rs`)
- Added a `layer_idx` to the `DeltaNetLayer` struct.
- Inserted debug statements to print layer-0 prefill statistics (`g` range, `exp(g)` range, `beta` range, and `conv_out` RMS).
- Added RMS logging for the embedding layer and the first 6 blocks during prefill (`embed`, `blk.0`, etc.).
- This was added explicitly to trace and diagnose "garbage output" generated by the Qwen3.5 implementation.

## Physics Impact

There are no functional changes to the physics steering logic in this commit. However, the debugging output in `qwen35.rs` is critical for stabilizing the model backend that generates the baseline hidden states upon which the physics forces operate.

## Key Findings

1. **Qwen3.5 DeltaNet Garbage Output**
   - Evidence: The commit message explicitly states that layer-0 stats were printed "for diagnosing garbage output".
   - Evidence: The telemetry log (`logs/latest.jsonl`) shows a generation attempt with the prompt "Hello". The generated token was `" بیش"` with a `steering_delta` of ~21.7.
   - Significance: The Qwen3.5 model backend is currently producing incoherent tokens. The new prefill stats (`g` and `beta` values) are the first step toward isolating whether the issue is numerical instability in the causal conv1d or gated RMSNorm.

## Experiment Progression

- **Before**: The TUI crashed on `--chat`, `cargo audit` would fail on transitive dev-dependencies, and Qwen3.5 failed silently with bad output.
- **After**: The TUI gracefully exits with an error, the audit policy is codified, and Qwen3.5 emits prefill layer stats.
- **Next**: Analyze the RMS and `g` values printed by Qwen3.5 to fix the underlying garbage output bug.

## Open Questions

- [ ] What specific layer in the Qwen3.5 `DeltaNet` is causing the RMS explosion/collapse that results in the garbage tokens?
