# Research Entry: Append human-readable log to logs/readable.txt after each run

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: `c51579779c0bd8a729008f3ef3fb7bad11ccbe92` | Date: 2026-03-01 | Author: ruffian-l

---

## Commit Summary

- **Message**: feat: append human-readable log to logs/readable.txt after each run
- **Files changed**: 2
- **Insertions/Deletions**: +239 / -2

## What Changed

This commit introduces a human-readable logging mechanism designed to simplify qualitative review, particularly on mobile devices. It writes to a new append-only text file (`logs/readable.txt`) in addition to the existing JSONL structured telemetry.

### `src/main.rs`

- What was modified and why: A new block was added to the end of `main()` to append run data to `logs/readable.txt`.
- Key code changes:
  - Opens `logs/readable.txt` with `create(true)` and `append(true)`.
  - Implements a manual Unix epoch to UTC date/time conversion to timestamp entries.
  - Formats output to include run timestamp, model variant, token count, active splat count, prompt, and full decoded text output.
- How this relates to the architecture in FOUNDATION.md: This is purely an observability improvement. It does not alter the Phase 2 continuous field or Phase 3 splat memory logic. It runs strictly after generation completes.

### `logs/readable.txt`

- What was modified and why: Created to backfill all prior existing JSONL log runs into a single, scrollable format.

## Physics Impact

There is no impact on the core physics pipeline. The commit only touches code execution after generation has finished.

## Key Findings

1. **Focus on Developer Ergonomics**
   - Evidence: The commit message specifically cites creating "One file to read all outputs, phone-friendly".
   - Significance: Indicates that rapid, qualitative review of model outputs (the "vibe check" of the physics steering) is currently a bottleneck.

## Experiment Progression

- **Before**: Reviewing outputs required parsing multiple `.jsonl` files per run or checking console stdout.
- **After**: All generated text and top-level metrics (splat count, token count, model variant) are consolidated chronologically in `readable.txt`.
- **Next**: Given the recent stabilization with force capping and the 5-prompt sweep infrastructure, this logging will likely be used to evaluate larger batches of steered generations.

## Open Questions

- [ ] Will `readable.txt` become unwieldy with hundreds of sweep runs, or is it strictly for short-term tailing?
