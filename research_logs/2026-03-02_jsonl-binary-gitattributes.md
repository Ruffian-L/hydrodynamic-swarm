# Research Entry: Fix JSONL Prompt Overflow in Jules-Action

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: `036047f206d588063d91bde9d4c3f2e1d446a4f7` | Date: 2026-03-02 | Author: ruffian-l

---

## Commit Summary

- **Message**: fix: mark JSONL as binary to prevent prompt overflow in jules-action
- **Files changed**: 1
- **Insertions/Deletions**: +3 / -0

## What Changed

Added `.gitattributes` to mark `*.jsonl` log files as binary.

### `.gitattributes`

- **What was modified and why**: Added a new file `/.gitattributes` containing `*.jsonl binary`.
- **Key code changes**: The file dictates git to treat JSONL files as binary.
- **How this relates to the architecture in FOUNDATION.md**: This is a purely operational/tooling change. As the telemetry dumps in `logs/*.jsonl` grew large during sweeping evaluations, they caused `jq: Argument list too long` (exit code 126) errors in the `jules-action` worker by bloating the `git show` output diff. Treating them as binary keeps diffs small while retaining the ability for Jules to read the logs directly from the repository.

## Physics Impact

There is no direct impact on the physical pipeline of the Hydrodynamic Swarm. This is a workflow fix to ensure smooth logging of the Diderot field operations.

## Key Findings

1. **Jules Logging Robustness**
   - Evidence: Marking `*.jsonl` as binary allows Jules to continue generating research lab notebook entries without crashing due to prompt token overflow.
   - Significance: Essential for tracking the large volumes of generated output and force metrics from continuous integration testing (e.g., Crucible suite).

## Experiment Progression

- **Before**: Full JSONL strings were getting serialized into diff dumps, crashing the workflow.
- **After**: Git ignores diffs for JSONL, fixing the overflow.
- **Next**: Continue automated documentation of experimental sweeps and physics experiments.
