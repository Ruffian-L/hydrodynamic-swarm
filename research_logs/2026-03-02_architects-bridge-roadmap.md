# Research Entry: Architect's Bridge Embedding Steering Concept

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: `06a9c1386e7d76049e63d8e7e779d9e5ba259d5d` | Date: 2026-03-02 | Author: Ruffian-L

---

## Commit Summary

- **Message**: docs(roadmap): add Architect's Bridge embedding steering concept to Phase 2.3
- **Files changed**: 1
- **Insertions/Deletions**: +6 / -0

## What Changed

Added a new theoretical framework for Phase 2.3 in the `ROADMAP.md` called **"Architect's Bridge: Tiny-Model Embedding Steering (Massive Compute Saver)"**.

### ROADMAP.md

- **What was modified and why**: Added 6 lines under Phase 2.3 to introduce a new approach to swarm intelligence.
- **Key logic added**: Instead of having smaller swarm models (like a 1B Llama 3.2 "Floor General" or a 384d gte-small "Historian") perform slow autoregressive generation to vote on tokens, these models will now output a single dense embedding vector. This vector is then instantly projected into the main 8B model's 4096d Niodoo latent space via a learned matrix (2048x4096 or 384x4096).
- **How this relates to the architecture in FOUNDATION.md**: This fundamentally changes the nature of the "swarm" from a discrete voting mechanism into a continuous, physical steering mechanism ("true gravitational steering"). This aligns perfectly with the North Star: "Retrieval and generation are the same physical process." The small models act as massive, distant Diderot attractors pushing the main particle down the continuous field, rather than distinct agents arguing over tokens.

## Physics Impact

- **Steering (niodoo.rs)**: This concept implies that in the future, the `goal_force` or `total_force` calculation during high-viscosity moments or Micro-Dreams will be heavily influenced by projected vectors from secondary models, rather than just the prompt embedding, splats, and field gradients. It turns the swarm into a physical force within the latent space.

## Key Findings

1. **Gravitational Steering over Voting**
   - Evidence: The roadmap notes, "This turns the swarm into true gravitational steering instead of slow voting." and states it "Runs in microseconds on Metal, zero latency impact on main 8B generation."
   - Significance: It proves the project is doubling down on physics-based steering over traditional LLM agentic voting, using linear algebra to map distinct latent spaces together for real-time physics interactions.

## Experiment Progression

- **Before**: Phase 2.3 envisioned a swarm of small models voting on generation paths, which would be computationally expensive.
- **After**: The swarm is reconceptualized as "gravitational steering" where small models generate single embeddings projected into the main model's Diderot space, drastically cutting compute time while maintaining influence.
- **Next**: Implement the learned projection matrices (e.g., 384x4096) and wire the "Floor General" and "Historian" models into the `NiodooEngine` during high-viscosity triggers.

## Open Questions

- [ ] How will the projection matrices (2048x4096, 384x4096) be trained or learned to effectively map the distinct latent spaces of Llama 3.2 1B and gte-small into the Niodoo 4096d space?
- [ ] What specific criteria will define the "high-viscosity moments" that wake up these secondary models?