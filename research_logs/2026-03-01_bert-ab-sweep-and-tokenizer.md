# Research Entry: bert A/B sweep complete + tokenizer fix

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: `9300938a1e6c0e0ae10391e74b85d5a15bac0eab` | Date: 2026-03-01 | Author: ruffian-l

---

## Commit Summary

- **Message**: feat: bert A/B sweep complete + tokenizer fix
- **Files changed**: 7
- **Insertions/Deletions**: +356 / -2

## What Changed

This commit finalizes the Phase 2 evaluation sweep by completing the 5-prompt run for the `bert` model variant and restoring the evaluation shell script `sweep.sh`. It also includes an important operational fix for the tokenizer symlink.

### logs/
- Five new `jsonl` telemetry files were generated for the `bert` variant across 5 distinct prompts.
- The human-readable aggregate log `logs/readable.txt` was updated with the decoded output of all 5 runs.

### sweep.sh
- The script was restored after an accidental deletion.
- A minor formatting fix was applied to the summary table: the "Tokens" column was replaced by "Model" to clearly differentiate the `unsloth` vs `bert` A/B runs.

### Tokenizer Symlink
- The tokenizer symlink was fixed (previously a 15-byte stub, now correctly linked to the real 9MB file). This ensures the correct decoding of generated tokens from the residual stream output.

## Physics Impact

How does this commit affect the core physics pipeline?

- **Steering (niodoo.rs)**: Telemetry from the `bert` variant confirms that the previously established force cap (`force_cap = 80.0`) is respected during the online steering process. In the multi-step generation runs, the maximum applied steering force (`delta_max`) peaked at `79.67` (in the bird migration prompt), keeping the particle stable as it slid down the Diderot field without exploding.
- **Generation (main.rs)**: The system maintained stability under `temperature = 0.9` and symmetric hyperparameters across variants (`viscosity = 0.6`, `splat_sigma = 150.0`, `splat_alpha = 2.0`).

## Key Findings

1. **Stable Divergence within Cap**
   - Evidence: Across the 5 prompts, `delta_mean` ranged from `45.33` to `52.32`, with `delta_max` ranging between `65.01` and `79.67`.
   - Significance: This indicates that the Diderot field exerts a consistent, moderate pull on the particle during text generation, maintaining trajectory influence without causing numerical instability. The `bert` model variant responds predictably to the continuous field interactions under the force constraints.

2. **Persistent Memory Stability**
   - Evidence: The number of splats before and after generation (`splat_count_before` and `splat_count_after`) remained constant during individual runs (ranging from 9 to 12 splats depending on the prompt), showing stable interaction with the `min_splat_dist = 100.0` parameter that prevents splat stacking.

## Experiment Progression

Where does this commit sit in the project timeline?

- **Before**: The `unsloth` model variant was evaluated, and the A/B test setup was prepared but pending the `bert` run.
- **After**: The 5-prompt evaluation sweep is now complete for both `unsloth` and `bert` variants.
- **Next**: Evaluate the stylistic and qualitative differences between the `bert` and `unsloth` variants (noted in `EXPERIMENTS.md`) to determine the impact of the Diderot field across different model topologies.

## Open Questions

- [ ] How do the physical characteristics of the `bert` field interactions correlate with its observed "poetic/physics-metaphor" style compared to `unsloth`'s analytical outputs?