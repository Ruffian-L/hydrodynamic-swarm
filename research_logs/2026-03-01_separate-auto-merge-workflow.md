# Research Entry: Separate Auto-Merge Workflow

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: `1d5dbc5e60345f95e9425151d4b82633b1816897` | Date: 2026-03-01 | Author: ruffian-l <223100882+Ruffian-L@users.noreply.github.com>

---

## Commit Summary

- **Message**: fix: separate auto-merge workflow triggered on PR open
- **Files changed**: 2
- **Insertions/Deletions**: +41 / -19

## What Changed

This commit separates the Jules automated PR merge functionality into its own independent GitHub Actions workflow to address asynchronous timing issues.

### `.github/workflows/automerge_jules.yml`

- A new workflow was created that specifically triggers on `pull_request` events with types `[opened, ready_for_review]`.
- Adds a robust security verification step that iterates through all files changed in the PR (`gh pr diff ... --name-only`). It explicitly blocks the merge if any file modified falls outside the `research_logs/` directory boundaries.
- Executes `gh pr merge --squash --admin` only if the PR safely passes the file verification step.

### `.github/workflows/jules_researcher.yml`

- Removes the previous `auto-merge` job that was tightly coupled to the `jules-action` workflow. This was necessary because Jules operates asynchronously, causing the previous merge job to fire before the PR had actually been generated and existed in the repository.

## Experiment Progression

Where does this commit sit in the project timeline?

- **Before**: The automated merge step was running sequentially in the same workflow as Jules. Since Jules creates PRs asynchronously, the merge step would execute and fail because the PR did not exist yet.
- **After**: The automated merge is now event-driven, responding appropriately only when Jules officially opens or readies a PR. It also introduces an explicit safety check ensuring Jules only modifies `research_logs/`.
- **Next**: Monitor the successful execution of the new `automerge_jules.yml` workflow to ensure Jules' research PRs are securely and reliably merged.
