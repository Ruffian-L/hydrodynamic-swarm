# Research Entry: Phase 2.2 Evaporation Engine & Hierarchical Splat Scaling

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: d0be37e829a13ab65b450dcd7b8f491ea41f1a2c | Date: 2026-03-03 | Author: ruffian

---

## Commit Summary

- **Message**: feat: Phase 2.2 evaporation engine -- time-degrading splat memory
- **Files changed**: 10
- **Insertions/Deletions**: +366 / -68

## What Changed

Introduces the Phase 2.2 evaporation engine, replacing static per-step decay with a robust time-degrading memory model and scale-aware splats based on the steering delta.

### Splat Memory (`src/splat.rs` & `src/memory.rs`)

- **New Attributes**: Splats now track `lambda` (decay rate), `created_at` (epoch seconds), `scale` (`Fine`, `Medium`, `Coarse`), and `is_anchor`.
- **Hierarchical Scaling (`Splat::with_scale`)**: Automatically adjusts `sigma` based on the steering delta magnitude. If `delta_norm > 30.0` it applies a coarse scale (multiplier 4.0), if `> 20.0` a medium scale (multiplier 2.0), else a fine scale (multiplier 1.0).
- **Time-Based Decay (`decay_step`)**: Switches from simple per-step multiplication to exponential decay `V(t) = V0 * exp(-lambda * dt)`. Pain splats decay asymmetrically at 70% of the normal lambda rate. Anchor splats never decay (`lambda = 0`).
- **Culling (`cull`)**: Automatically purges splats where `|alpha|` drops below the pruning threshold, keeping the memory file lean and optimizing `query_force` compute.
- **Backward Compatibility**: `load` handles missing fields from v1 safetensors by applying default values (e.g., `lambda = 0.02`).

### Generation Loop (`src/main.rs`)

- Replaced static `splat_sigma` assignment with `Splat::with_scale` to inject scale-aware splats.
- Explicitly calls `engine.memory_mut().decay_step` and `cull` after generation, but before saving.

## Physics Impact

- **Steering (main.rs)**: The multi-scale approach means large trajectory jumps (steering delta > 30.0) create broader structural scars (`Coarse` scale), while small corrections create precise, fine-grained bumps.
- **Memory (memory.rs)**: Splat forces now naturally "evaporate" over actual time rather than just generation steps. This creates a temporal dynamics dimension where recently learned facts exert stronger forces than older ones, unless they are anchored.

## Key Findings

1. **Evaporation & Delta Extremes**
   - Evidence: Telemetry from the newly introduced logs (`11-12-24`, `11-14-00`, `11-15-41`) shows mean steering deltas of 149.78, 195.17, and 110.23 respectively, with max steering deltas reaching 381.68, 338.22, and 229.69.
   - Significance: The delta values frequently exceed the 30.0 threshold, meaning the system is successfully utilizing the `Coarse` hierarchical scale. The new culling logic will be critical in managing the sheer number of coarse splats generated during these intense steering events.

## Experiment Progression

- **Before**: Splats decayed per generation step, regardless of elapsed time. `sigma` was static or determined by simple hardcoded branches.
- **After**: Phase 2.2 implements true temporal evaporation. Splat creation is now dynamically scaled to match the magnitude of the steering event.
- **Next**: Evaluate the impact of different `lambda` configurations on long-term knowledge retention and the overall size of the splat memory file.

## Open Questions

- [ ] Does the asymmetric decay rate (70% for pain) effectively prevent "catastrophic forgetting" of negative trajectories during long periods of inactivity?
