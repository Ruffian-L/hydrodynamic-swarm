# Research Entry: Core Steering Loop Stabilization

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: 89cfd0da1fa0754f63682db89ad3b846bd7c0db0 | Date: 2026-03-03 | Author: Ruffian-L

---

## Commit Summary

- **Message**: fix: stabilize core steering loop -- three critical fixes
- **Files changed**: 11
- **Insertions/Deletions**: +182 / -17

## What Changed

### Diderot Field Initialization (`src/field.rs`, `src/llama.rs`, `src/main.rs`)

- Replaced loading `universe_domain.safetensors` with extracting live embeddings directly from the loaded `ModelWeights` (`llama.token_embeddings()`).
- Added `ContinuousField::from_embeddings()` to build the field directly from the model's token embedding matrix `(vocab_size, hidden_dim)`.
- It now auto-tunes `sigma` based on sampled pairwise distances.
- **Why**: The previous `universe_domain.safetensors` was a 2GB file of all zeros. The Diderot field has been dead since day one, meaning ridge-running forces were never actually applied. This ensures the field is perfectly aligned with the actual loaded model.

### Hidden-State Renormalization (`src/niodoo.rs`)

- Added renormalization step to the `steer()` function in `NiodooEngine`.
- Rescales the steered hidden state to match the norm of the baseline residual state.
- **Why**: Prevent cumulative drift off the Llama manifold. Without this, applying continuous steering forces drifts the hidden state norm, which previously caused the `lm_head` to produce garbage output after 40-80 tokens.

### Conservative Physics Defaults (`src/config.rs`)

- Changed `PhysicsConfig` defaults:
  - `force_cap`: 35.0 -> 8.0
  - `dt`: 0.08 -> 0.04
  - `viscosity_scale`: 0.35 -> 0.15
  - `gradient_topk`: 2048 -> 512
- Maximum per-step perturbation per dimension is dramatically reduced from 2.8 to 0.32.

## Physics Impact

- **Field (`src/field.rs`)**: Now actively applying ridge-running forces based on accurate, live embeddings from the loaded model instead of a zeroed-out field.
- **Steering (`src/niodoo.rs`)**: `dt` is reduced, slowing the particle's movement. `viscosity_scale` is reduced, providing less drag/resistance against the gradient force. The hidden state is forced back to its original baseline norm magnitude, meaning forces now only change the *direction* of the particle, not its magnitude.
- **Generation (`src/config.rs`)**: Slower, more controlled trajectory through the Diderot field due to smaller `force_cap` and `dt`.

## Key Findings

1. **Dead Field Discovery**
   - Evidence: `universe_domain.safetensors` was all zeros.
   - Significance: The entire first phase of the project ("Phase 2.1 Hidden-state steering") was operating without actual Diderot ridge-running forces.
2. **Steering Magnitude Impact**
   - Evidence: Telemetry from `logs/2026-03-03_11-37-08_unsloth_v3-forcecap35_T0_9_s35_a2_d100.jsonl` shows `steering_delta` values climbing up to `331.4896`, while `residual_norm` climbs to `1311.6476` across 200 tokens.
   - Significance: Applying unbounded forces was dramatically inflating the hidden state vector. The new renormalization ensures the norm remains consistent, preventing catastrophic generation collapse after long sequences.

## Experiment Progression

- **Before**: Steering forces were applied blindly, often resulting in vector norm inflation, causing the model head to emit gibberish. The Diderot field was inactive due to empty data.
- **After**: The Diderot field is now live, pulling particles towards dense regions of the embedding manifold. The steering forces are smaller, more precise, and constrained to the surface of a hypersphere defined by the baseline hidden state norm.
- **Next**: Evaluate the long-term coherence of generated text over 500+ token trajectories with the active Diderot field and normalized hidden states.

## Open Questions

- [ ] Does the auto-tuned `sigma` based on pairwise distance provide the optimal gradient slope for the ridge-running force?
- [ ] How does restricting the steering force to only change the direction (via renormalization) interact with the model's natural hidden state trajectory?
