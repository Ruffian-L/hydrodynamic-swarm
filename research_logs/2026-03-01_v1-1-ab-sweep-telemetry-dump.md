# Research Entry: Full Telemetry Dump of v1.1 A/B Sweep

> Auto-generated by Jules (Repo Groundskeeper)
> Commit: 57e2d039dadee079b2ead8adcba291ef1350fd44 | Date: 2026-03-01 | Author: ruffian-l

---

## Commit Summary

- **Message**: docs: full telemetry dump of v1.1 A/B sweep (plain text)
- **Files changed**: 1
- **Insertions/Deletions**: +826 / -0

## What Changed

Added a comprehensive plain text telemetry dump of the recent v1.1 A/B sweep to the `logs/` directory.

### `logs/experiment_v1.1_sweep.txt`

- A new log file was added that contains the full telemetry, configuration, and generation output for the 5-prompt A/B evaluation sweep.
- Includes step-by-step metrics tracking delta, residual, and token generation across runs comparing `unsloth` and `bert` model variants.
- The recorded configuration: `sigma=150`, `alpha=2.0`, `force_cap=80`, `T=0.9`, `min_dist=100`.
- The log also captures that Micro-dreams were enabled and triggered every 8 steps with a 2-step forward projection and a blend of 0.10.

## Physics Impact

This commit is purely documentation of the physical pipeline's state during the sweep, but it provides key visibility into the forces:

- **Steering (niodoo.rs)**: The telemetry details the applied forces via `delta`, showing the continuous impact of the continuous Diderot field (`force_cap=80`, `T=0.9`) on the residual stream at each token generation step.
- **Memory (memory.rs/splat.rs)**: Splat formations are recorded, noting the specific counts generated for each prompt. For instance, `unsloth` generated 14 splats on the "transformer attention" prompt, and `bert` generated 9 on the "consciousness" prompt.
- **Dreaming (dream.rs)**: The log notes `Micro-dreams: ON`, validating the real-time consolidation feature added in `v1.1`.

## Key Findings

1. **Stable Physical Divergence Across Architectures**
   - Evidence: The mean `delta` remains remarkably consistent between the two distinct models. `unsloth` ranged from `41.18` to `52.81` and `bert` ranged from `45.34` to `52.33`.
   - Significance: The hydrodynamic swarm steering mechanism demonstrates stable and predictable physics, maintaining a roughly `~50` delta mean on the residual stream regardless of the underlying LLM architecture when using the established configuration values.

2. **Consistent Splat Memory Generation**
   - Evidence: The generated splat count (scar tissue) remains comparable across prompts. The `unsloth` model formed between 7 and 14 splats, while `bert` produced between 9 and 12 splats.
   - Significance: The Diderot field triggers micro-dream consolidation at a stable rate, indicating that the `min_dist=100` parameter is robust at creating memory topology without catastrophic over-accumulation.

## Experiment Progression

This commit directly supports Phase 2 evaluation goals.

- **Before**: Logs were individually recorded in JSONL format, making holistic comparisons of the 5-prompt sweep difficult to read.
- **After**: A single, consolidated plain-text telemetry dump allows for rapid analysis of the complete v1.1 A/B sweep, comparing `unsloth` and `bert` outputs side-by-side.
- **Next**: Analyze the generated text outputs documented in the log for qualitative improvements in coherence, and iterate on the `sigma` and `force_cap` constants.

## Open Questions

- [ ] Does the higher number of splats on technical prompts (e.g., 14 for `unsloth` on "transformer attention") correlate with a higher quality generated output, or does it indicate increased instability in the field?