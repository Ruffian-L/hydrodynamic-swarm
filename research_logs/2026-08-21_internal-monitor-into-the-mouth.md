# Internal monitor into the mouth

> Date: 2026-08-21
> Agent: Grok (xAI)
> Repo: hydrodynamic-swarm-3surface

## Context

Jason: hydro needs Niodoo’s `[Internal monitor: high entropy due to … | H0= H1= loop= overfire=]` ASAP. Code mutation pairs CHANGELOG in the same turn.

Niodoo `runs/2026-08-21_official10_tagsfirst` Prompt 1 (snail) spliced measured TDA into the mouth on a closed cycle around `"meters"`, then she emitted `<focus>`. Hydro SYS only said “TDA is internal eyes.” No line ever fired.

Same locked opening + Prompt 1 on hydro `20260821_100439` (pre-mutation binary): packing still printed `emit exactly one tag`, no `[Internal monitor]`, no tag, snail day-loop.

## Hypothesis

A measured Internal monitor line in the mouth, plus April when+what tag table first and no “do not”, lets her read loop pressure and pick a hand the way Niodoo did on P1. Quiet windows stay quiet. Inject uses a real decode forward so KV/RoPE stay in sync (Niodoo’s `cannot broadcast` was append-without-forward).

## What changed

- `GOD_TIER_SYSTEM` / `prompts/god-tier-system-control.txt`: April when+what table first (`Use when you are stuck in a loop` …). MIRROR teaches the measured `[Internal monitor: …]` line. Dropped “TDA is internal eyes.” No “do not.” Several tags still fine. Spoken remember stays. Long-form `key=value` still accepted.
- New `src/tda_monitor.rs`: rust Vietoris–Rips on the last 32-token decode window (entropy / margin / residual / splat / p_top1 / step). Same mouth format as niodoo-live `conversation_monitor_warning`. Named loop of a word ≥3 is enough. No `would_focus` / ACTION. Quiet stays quiet. Basin-split needs margin collapse or repetition so unique tokens do not strobe (Niodoo’s overfire-alone lesson).
- Decode inject: print the line, push it into `pieces` (next-turn history), and **forward each inject token**. Cooldown 48. `HYDRO_TDA_MONITOR=0` off. Soup settle waits one step if a monitor is pending so she can read it this turn. Monitor tokens are not decode-trail scars.
- Packing print is already `available tags table` in source; 100439 was the old binary (`emit exactly one tag`).
- Same-prompt files: `evals/side-by-side-p1/prompts.txt` (Official opening + Prompt 1, not a new Official 10 stamp).
- Units: `repeating_word_emits_closed_cycle_monitor`, `unique_tokens_stay_quiet`, `circle_points_have_h1`, `god_tier_system_is_system_turn_and_forbids_narration`.
- Did not re-run Official 10. Did not move α.

## Findings

- Niodoo tagsfirst P1: Internal monitor on `"meters"` then `<focus>`. Opening also `<explore>`.
- Hydro 100439 (old SYS, no inject): no monitor, no tag. Side-by-side miss was the missing mouth line, not the snail prompt.
- Hydro 101526 matched the mouth hypothesis. Packing `PRESENT (available tags table)`. Opening `<focus>` with no monitor (SYS table, not TDA). P1 looping snail: `[Internal monitor: … closed cycle around "Step" | H0=32 H1=465 loop=0.43 overfire=0.12]` then `<spike>` (blend=6.50, rep inverted). Later `[Internal monitor: … "Day" | H0=32 H1=465 loop=0.40 overfire=0.13]` then `<reset>`. She also imitated a garbled monitor (`"Day 1" | H0=H1 loop=0.89`) — mouth theater on top of the inject, same family as Niodoo.
- H1=465 is loud vs Niodoo P1 H1~10–21. Named-word cycle still discriminated. `[CONTROL_RECEIPT]` still splices into stdout inside `<focus` `>`.

## Next

Do not stamp Official 10 unless Jason says go. Do not move α. H1 bar count vs Niodoo is the next measurement if we care about the numbers, not the mouth. `[CONTROL_RECEIPT]` stdout splice is still hiding the hand the same way the old mask did. Memory channel extra-sentence miss is still Niodoo-side.

Signed: Grok (xAI) · operator Jason
