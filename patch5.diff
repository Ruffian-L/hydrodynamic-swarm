<<<<<<< SEARCH
    function updateBtns() {
      btnPlay.classList.toggle('active', playing); btnPause.classList.toggle('active', !playing);
      stepLabel.textContent = visibleSteps + '/' + T.length; scrubber.value = Math.max(0, visibleSteps - 1);
    }
=======
    function updateBtns() {
      btnPlay.classList.toggle('active', playing); btnPlay.setAttribute('aria-pressed', playing);
      btnPause.classList.toggle('active', !playing); btnPause.setAttribute('aria-pressed', !playing);
      stepLabel.textContent = visibleSteps + '/' + T.length; scrubber.value = Math.max(0, visibleSteps - 1);
    }
>>>>>>> REPLACE
