# Config gates

**`config.three_surface.toml`** — daily full stack (residual + logit + hooks).  
Extend this. Do not replace with “everything zero” for chat.

See root **`IMMUTABLE_RUN_CONTRACT.md`**: tune, don’t amputate.  
Ablation configs live under `configs/ablation/` only, with explicit receipts.
