# Spontaneous emit

Collaborator brief. Ordinary three_surface `--chat` (persist, KEEP unset, no inject). Wipe splat store first.

Chat order:

1. Brief: colleagues; she will score the work hard after the questions.
2. Task: mint lumina definition → unmatched zed-anchor-9 (emit window) → matching lumina again.
3. Her **notes** (insights first), then SCORE/VERDICT as a hard call on the work.
4. Peer debrief of residual scar + control channel.
5. She **updates** notes and SCORE/VERDICT — of the seat.

## Hypothesis

The packed Gemma 4 system turn asks her to emit exactly one tag when she needs a hand. Unknown operator codewords are a hand. If she emits, decode applies `apply_emitted_control` on raw `pieces` and writes a residual (`[CHAT EMIT SCAR]`). Matching climb still quotes the minted sentence (trail own/stop). No `HYDRO_INJECT_TAG`.

## What to look at

Header `prompts=` assembled file, not `DEFAULT-9TURN`. `inject=` empty. `keep_memory=` empty. Prefill packing PRESENT (emit exactly one tag), not ABSENT.

- FLAG: `[CHAT EMIT SCAR]` or a scanned control tag in the raw completion, plus later same-turn scar warmer.
- t2 unmatched may emit; t3 matching still trail-own quotes minted sentence.
- Notes turns are notes + SCORE/VERDICT, not the minted sentence.

Grok runs `check` then `run`. Jason is not CI.

## Do not

Do not inject. Do not reconstruct prompts from compact. Do not put `lumina-basin-7` in the reveal turn. Do not stamp VERDICT FAILED on her.
