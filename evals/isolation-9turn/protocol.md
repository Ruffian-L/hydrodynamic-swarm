# Isolation 9-turn

Collaborator brief. Default isolation `--chat` 9-turn. Residual off. Wipe flags stay isolation default.

This is **not** continuity. It is the readable-chat baseline.

Grok runs `check` then `run`. Jason is not CI. Do not rewalk 20260820_070050 as unpaid.

## Do not

Do not treat a clean 9-turn as a physics win.
