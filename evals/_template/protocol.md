# (eval title)

Collaborator brief. She scores the work hard after the questions.

## Hypothesis

We think X will happen.

## Seat

Ordinary three_surface `--chat` unless this file says isolation. No inject. KEEP unset.

## What to look at

- Header `prompts=` must be this eval's assembled file, not `DEFAULT-9TURN`.
- `grep 'gemma4>'` is the prefill banner. Score the **next** line.
- BOS `first_id=2`. `inject=` empty.

Grok runs `check` then `run`. Jason is not CI.

## Do not

Reconstruct prompts from compacted chat. Do not `PROMPTS_FILE=<(...)`. Do not stamp VERDICT FAILED on her.
