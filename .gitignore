# Rust build artifacts
target/
# Keep vendored offline sources + cargo vendor replace config for worktrees/clones
.cargo/*
!.cargo/config.toml
**/*.rs.bk

# Legacy data directory (model blobs stay local; SHA256SUMS is committed)
data/*
!data/google/
data/google/*
!data/google/SHA256SUMS
*.sha256.verified
project_brain_dump.json
.ai_sweep.pid

# OS files
.DS_Store
**/.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Project specific
exhibits/

# Local-only museum recordings (checked-in demos live under tools/museum/demos/)
tools/museum/demos/custom-latest.viz.json
tools/latest_demo.viz.json

# Experiment / runtime logs — do NOT commit noise
# Public milestones: tools/museum/demos/*.viz.json + catalog.json
logs/**
logs/taco.db
*.db

# Temporary files and directories
*.tmp
*.temp
/tmp/

# Local machine config (profiles like config.27b.toml ARE committed)
# Anchor to repo root so .cargo/config.toml is not ignored
/config.toml

# Model files (use LFS for large files)
*.gguf
*.bin

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env

# Node modules
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Jupyter
.ipynb_checkpoints/

# macOS
.AppleDouble
.LSOverride

.aider*
.quality-gate-failed
logs/reviews/

# local bridge prompt registry / runtime memory stores
data/bridge_prompts.json
data/splat_memory.*

# Internal operator / team docs (not for public tree)
LOAD.md
SHEP_FORK_HANDOFF.md
TEAM_GOAL*.md
*_HANDOFF.md
research_logs/*HANDOFF*
research_logs/*SESSION_CATCHUP*
research_logs/*CONTINUITY_FIRST*
AGENTS*.md
AGENTS(jules).md
.agent/
.jules/
.grok/

# editor/backup cruft
*.bak
*.old
Untitled
